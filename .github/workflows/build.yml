name: Build and push latest image

on:
  push:
    branches:
      - main
    paths:
      - 'app-code/**'
  workflow_dispatch:

jobs:
  Build-and-push-image:
    name: Build and push Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: |
          cd app-code
          docker build -t "${{ secrets.DOCKERHUB_USERNAME }}"/flask-app:latest -t "${{ secrets.DOCKERHUB_USERNAME }}"/flask-app:${{ github.run_id }} .

      - name: Push Docker images
        run: |
          docker push "${{ secrets.DOCKERHUB_USERNAME }}"/flask-app:latest
          docker push "${{ secrets.DOCKERHUB_USERNAME }}"/flask-app:${{ github.run_id }}
